// Re-export from the canonical schema definition in calls/schemas
export { ICallParticipant, ITransferEntry, ICallRecording, IQualityMetric, ICall, CallSchema } from '../../calls/schemas/call.schema';
